# v0.2.99 test release

- align the libvirt power-state normalizer name with the adapter call sites and parser tests;
- retain the cross-platform virtualization adapter layer and the validated nested KVM/libvirt/QEMU coverage.
