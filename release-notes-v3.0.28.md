# v3.0.28 测试版

- 缩减桌面安装包：移除已由 Vite 打包的重复依赖及发行包中的源码映射，保留 Agent、硬件探测和所有语言资源。
- 可选 Recharts 图表按需加载，降低普通页面首次加载成本。
- Windows 会话探测改为异步且合并并发请求，避免阻塞主进程。
- 隐藏窗口暂停 UI 轮询，恢复显示立即刷新；后台 Agent 采集上报继续运行。
- 离线缓存复用内存，每分钟持久化并在正常退出时刷新；异常退出时磁盘缓存可能落后一轮持久化周期。
- 桌面独立读取请求并行执行，更新元数据缓存 15 分钟；Web 推送不再重复请求整份历史快照，历史按所选刷新间隔更新。
- 增加性能回归测试与同一 Windows runner 上的旧/新版 Release 量化对比工作流。

这是测试版，不更新 latest，也不部署生产。性能数字及验收边界见 docs/performance/2026-09-19-audit.md 与后续结果记录。
