# v0.2.296 测试版

- 为已有 NAS 测试部署兼容旧的 6 位 `ACCESS_KEY`；稳定渠道仍要求至少 32 位。
- 测试渠道兼容已有的旧 MySQL 凭据，部署迁移会从运行中的 MySQL 容器补齐缺失的 Compose 变量。
- Compose 迁移统一把 Hub 到 MySQL 的连接改为内部服务地址 `mysql:3306`，不改变现有 MySQL 数据卷。
- 本版本是测试版，不代表正式生产发布。
