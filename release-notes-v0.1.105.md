# v0.1.105

## Changes

- Display the current release version in the web console, Windows GUI, and Android GUI.
- Include the release version in Go and Node agent startup logs.
- Read the web release version from the repository VERSION file during build.
