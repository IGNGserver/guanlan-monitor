# v0.2.241

## 双路 CPU Socket 遥测

- Agent 按物理 CPU Socket 采集独立的实时使用率、频率和温度。
- 中枢保留每个 Socket 的独立数据，不再把整机汇总值伪装成每颗 CPU 的读数。
- 设备详情、硬件摘要和通用 CPU 小组件显示 Socket 编号，便于区分双路处理器。
- 单个 Socket 的数据无法获取时显示“无法获取数据”，不再用 0 填充曲线。
