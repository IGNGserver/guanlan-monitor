# v0.2.255

## Windows GPU 资源回退

- 首次检测到 GPU 子进程异常退出时，桌面端自动重启并切换到软件渲染。
- 软件渲染回退实例不会重复重启，避免 GPU 故障与系统资源紧张叠加成闪退循环。
- 保留 GPU/渲染进程诊断日志，便于区分 GPU 资源故障、渲染器 OOM 与主进程异常。

## 校验

- 构建、打包、Release 资产和 workstation 安装验证由 GitHub Actions 流程执行。
