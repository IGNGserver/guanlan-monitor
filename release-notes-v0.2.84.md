# v0.2.84

## 修复

- 修复本机 Agent 页面因后端返回空的探测集合而黑屏的问题，并增加运行时错误兜底。
- 修复 Agent 探测计划中 `gopsutil` provider 无法显示的问题。
- 统一桌面窗口、任务栏、托盘与安装快捷方式使用观澜图标。
- 将 Windows 图标升级为包含 256×256 与常用小尺寸的多分辨率 ICO，避免系统回退到 Electron 默认图标。
