# 观澜 v3.0.1 测试版

这是观澜 Web / Electron 共享 UI v3 的结构性返工测试版。

- 修复宽屏 Web 主内容落入零高度网格行导致的空白，并让 1440/1024/840/820/390 宽度共用明确的主布局契约。
- 修复总览健康统计被普通设备/虚拟机局部筛选污染的问题；全局统计覆盖所有实例，VM 电源状态与宿主机 Agent 状态分别呈现。
- 将 WorkspaceApp 收敛为组合根，页面、共享视图辅助件、Context 类型与页面/详情/设置 CSS 分离。
- 设备详情增加“设备 > 主机名”面包屑和稳定事实条；Widget 编辑改为显式编辑、添加、拖拽/尺寸、撤销重做、保存与放弃草稿。
- 设备顺序改为可取消的草稿，设置在 390px 提供分类导航与返回路径，全局搜索覆盖设备、页面和设置。
- Actions 增加共享 UI 行为门禁、几何断言及两轮视觉矩阵，并在 Windows runner 对 Release setup 执行静默安装、启动和窗口证据验收。

本版本仅作为 GitHub prerelease 测试版发布，不更新 `latest`，不用于生产部署。
