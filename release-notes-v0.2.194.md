### 产品逻辑与桌面端修复

1. 明确实时、缓存和离线状态：离线缓存显示真实缓存时间并进入只读模式，避免把过期数据误认为刚同步，也避免在缓存状态下删除实例、调整顺序或修改小组件布局。
2. 拆分“退出桌面查看”和“停止本机上报”：前者只结束当前中枢认证，后者会停止 Agent、关闭云同步并清除本机上报凭据；Agent 重启改为单次原子操作并提供处理中反馈。
3. 修复普通设备与虚拟机的计数、列表和总览图表筛选语义，统一使用“实例”表达，并让总览与明细共用实际时间范围。
4. 修复多系列遥测图表的时间戳对齐、无样本状态切换和触摸提示交互；磁盘总览改为总已用容量，网络流量增加日/周/月流量日历。
5. 补齐 Agent 身份、采样节奏、采集/上传开关、探针来源、实例上报开关和实例级指标覆盖配置；增加风扇备注入口并修正 Linux hwmon 与 Windows LibreHardwareMonitor 的来源标识。
6. 统一桌面端与 Web 端的控制台适配边界，替换主要的原生删除/输入弹窗，并补充版本、缓存和交互状态说明。
