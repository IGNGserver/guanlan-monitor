# 观澜 v3.0.11 测试版

这是观澜 Web / Electron 共享 UI v3 的登录态行为断言修复测试版。

- 将登录表单行为测试从严格的完整 label 文本改为匹配访问密钥字段的可访问名称，兼容字段说明文本仍属于同一表单控件语义的实现。
- 保留完整登录、空态、在线、会话过期状态切换与两轮视觉矩阵门禁。
- 保留 v3 的全部页面、Context、CSS、健康统计、Widget 编辑、设备顺序草稿、390px 设置导航、全局搜索及数据/权限契约。

本版本仅作为 GitHub prerelease 测试版发布，不更新 `latest`，不用于生产部署。
