# 观澜 v3.0.8 测试版

这是观澜 Web / Electron 共享 UI v3 的全局搜索焦点修复测试版。

- 修复 CommandPalette 在 Web 中打开时前焦点落到 `body` 的情况；关闭后回退并恢复到全局搜索触发器。
- 保留 Escape 关闭、搜索设备/页面/设置、深链跳转和焦点恢复行为门禁。
- 保留 v3 的页面、Context、CSS、健康统计、Widget 编辑、设备顺序草稿、390px 设置导航、两轮 40 格视觉矩阵及数据/权限契约。

本版本仅作为 GitHub prerelease 测试版发布，不更新 `latest`，不用于生产部署。
