# v0.2.190

### 修复与优化
1. **桌面端容量与显存指标单位格式化优化（显示小数）**：
   - 优化桌面端容量格式化函数（`formatBytes`），将 GB、MB、KB、TB 单位在数值大于或等于 10 时原本截断为整数的规则调整为保留 1 位小数（如 `16.0 GB`、`31.9 GB`、`245.8 GB`、`512.0 GB`）。
   - 统一全套桌面端工作区组件、仪表盘总览卡片、设备指标抽屉以及自定义 Widget 网格图表中的容量精度展示，让物理内存、已提交内存、页面文件、硬盘容量及显卡显存等关键指标显示更细致、精确。
