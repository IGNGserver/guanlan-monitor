# v0.2.187

### 改进与优化
1. **重构并统一小组件删除逻辑，移除废弃的“隐藏”与已隐藏托盘**：
   - 前端卡片操作栏统一提供“删除”按钮，移除了历史遗留的“隐藏”按钮与双轨逻辑。
   - 移除顶部编辑工具栏中已废弃的“已隐藏 N 项”折叠托盘（Tray）及对应样式。
   - 精简网格布局与避让引擎（`widgetGrid.ts`），剥离所有 `placement.hidden` 遍历与过滤分支，直接以物理坐标进行紧凑排布与碰撞计算。
   - 后端及共享数据模型（`@dsc/server` 与 `@dsc/shared`）将 `hidden` 属性平滑放宽为可选兼容字段，确保向后兼容历史保存数据的同时彻底解除对隐藏状态的强依赖。
