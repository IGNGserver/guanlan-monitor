# v0.2.251

## 修正 Proxmox 集群存储池展示

- 过滤 Proxmox 集群下发但当前节点未启用、或没有容量数据的存储配置，避免将其他节点专属的 `pve-ssd` 和不可用的 `NAS-PVE` 展示为当前设备的存储池。
- 当前状态、历史序列和旧版本兼容路径统一使用同一套可用存储池过滤规则。
- 将虚拟化存储池设备卡片移动到网卡和物理硬盘卡片之后，物理硬盘作为存储区域的主体展示。

## 校验

- 构建、测试、打包、Release 资产和部署验证由 GitHub Actions 流程执行。
