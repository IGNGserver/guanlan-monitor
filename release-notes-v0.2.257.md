# v0.2.257

## 修复安卓端存储池显示

- 修复 Android 端因未映射存储池 API 字段，导致 `local-lvm`、`pve-ssd` 等存储池无法显示的问题。
- 保留当前容量、可用空间、使用率和历史曲线；Proxmox 未提供的存储池读写速率继续显示“无法获取数据”。
- 增加存储池数据模型测试，避免当前数据与历史序列合并时再次回归。

## 校验

- 构建、测试、打包和 Release 资产由 GitHub Actions 流程执行。
