# v0.2.212 测试版

## 温度源与硬件指标

- 本地 Agent 硬件探测新增温度源探针结果，展示 CPU、GPU、核显共享 CPU Package、硬盘、主板/供电及其他可用传感器的原始来源、当前值、阈值和状态。
- 面板小组件目录新增“全部温度源”，可单独添加到自定义面板。
- GPU 设备组和小组件补齐编码负载、解码负载、核心频率和驱动信息；保留核心占用、显存和温度。
- GPU 自动迁移指标包含驱动信息，温度源仍按原始传感器保留，不做跨源平均。

## 验证范围

- 静态检查由本地完成。
- Go、桌面端、Web 端构建和打包由 GitHub Actions 执行。
- 发布后使用 workstation 安装包验证本地 Agent 探测，并使用 pve1 的真实采集链路复核温度源。
