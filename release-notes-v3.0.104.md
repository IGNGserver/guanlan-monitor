# 观澜 v3.0.104 测试版

## 修复中枢残留虚拟机设备

- 设备注册入口拒绝 `vm:` 标识：中枢启动时的旧数据清理与首次设备列表请求存在竞态，清理尚未完成时列表请求会把 `vm:` 标识重新写回设备表，此后 Redis 与历史记录都已清干净，这些行却再没有任何路径会清除，表现为移除虚拟机功能后凭空多出的一批"正常设备"。
- 设备列表查询在数据层过滤 `vm:` 标识，清理完成之前也不会把残留设备展示出来。
- 实时设备列表同样过滤 `vm:` 标识；清理逻辑改为读取未过滤的原始集合，避免过滤把待清理条目对清理逻辑自身隐藏掉。
- 删除设备不再可能为 `vm:` 标识新建记录。

本版本仅作为 GitHub prerelease 测试版发布，不更新 `latest`，不用于生产部署。
