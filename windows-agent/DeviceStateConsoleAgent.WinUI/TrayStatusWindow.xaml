<Window
    x:Class="DeviceStateConsoleAgent.WinUI.TrayStatusWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="RootGrid" RequestedTheme="Default" Background="Transparent">
        <Border x:Name="MenuSurface"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16"
                Opacity="0">
            <Border.RenderTransform>
                <TranslateTransform x:Name="MenuTransform" Y="10" />
            </Border.RenderTransform>
            <StackPanel Spacing="10">
                <TextBlock Text="观澜" FontSize="16" FontWeight="SemiBold" />
                <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" />
                <StackPanel Spacing="6">
                    <TextBlock Text="{Binding TrayMonitorStatusText}" Style="{StaticResource BodyTextBlockStyle}" TextWrapping="Wrap" />
                    <TextBlock Text="{Binding TraySubmitStatusText}" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="Wrap" />
                </StackPanel>
                <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" />
                <StackPanel Spacing="4">
                    <Button Content="打开主面板" Click="OpenButton_OnClick" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" />
                    <Button Content="退出" Click="ExitButton_OnClick" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
