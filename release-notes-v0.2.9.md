# Device State Console v0.2.9

## 测试版

### 修复

- 修复 Intel 核显被 WMI 与 LibreHardwareMonitor 实例 ID 不一致过滤的问题。
- 增加 Intel `D3D` GPU 利用率和 `SmallData` 共享显存采集支持。
- 修复 Windows GUI 开启显卡采集后未自动启用 GPU 指标上传的问题。
- 增加 Intel GPU 传感器映射回归测试。

这是测试版发布，不作为稳定安装源或生产环境依据。
