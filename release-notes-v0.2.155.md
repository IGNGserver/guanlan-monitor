# v0.2.155

- 新增 `dsc` CLI UI，可通过终端菜单调整中枢连接、采样、指标、硬件探针和采集器运行状态。
- CLI UI 支持 `status`、`doctor`、`config`、`start`、`stop`、`restart` 和 `shutdown` 等无界面命令。
- CLI Release 新增固定版本的 `install-cli.sh` 与 `install-cli.ps1` 一键安装入口，并在安装包中携带本地 backend。
