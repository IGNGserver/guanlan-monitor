# v0.2.182

### 改进与修复
1. **修复折线图断点逻辑与连续线渲染**：
   - 优化断点判定算法，基于采样点间隔的中位数与时间跨度智能判定真实离线断点，修复了正常连续采样点被误判为空白断点而绘制为孤立散点的问题。
2. **分离图表与详细信息（禁用手势滚轮翻页，提供独立按键切换）**：
   - 彻底移除小组件卡片内拦截滚轮和指针滑动手势翻页的机制，锁定卡片内垂直滚动，确保页面整体滚动顺畅。
   - 在图表卡片右上角增加“详细信息”/“返回图表”切换按钮，点击即在图表与统计文本详情页之间无缝切换。
3. **小组件目录分离折线图与饼图独立类型**：
   - 在组件目录中为具有上限的指标（CPU使用率、内存使用、磁盘容量、GPU负载、GPU显存使用）提供明确独立的“折线图”与“饼图”组件项供添加。
   - 移除卡片内常驻的视图类型切换按钮，使小组件固定以所添加的形式进行展示。
