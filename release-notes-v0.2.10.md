# Device State Console v0.2.10

## 测试版

### 修复与改进

- 修复显卡采集已开启但旧配置缺少 GPU 指标，导致显卡数据全部为 0 的问题。
- 启动 backend 时自动迁移缺失的 GPU 指标配置，并保留用户已有的显卡指标选择。
- 为 CPU、磁盘、网卡和显卡实例增加右侧“配置”入口。
- 支持按实例独立开关使用率、温度、频率、显存等检测项。
- 增加 GPU 配置迁移回归测试。

这是测试版发布，不作为稳定安装源或生产环境依据。
