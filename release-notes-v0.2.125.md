# v0.2.125 测试版

## 综合硬件遥测与磁盘实例修复

- 综合面板改为 CPU、硬盘、网卡、显卡的全实例平均趋势，并在图表底部列出参与采集的具体型号。
- CPU、硬盘、网卡、显卡明细选项卡按实例分别展示独立图表，补充 CPU、硬盘和网卡型号信息。
- 修复历史聚合数据无法恢复 CPU、网卡和风扇实例序列的问题，谷值改为真实采样最低值。
- Windows 磁盘探测和采集增加 gopsutil 分区回退与 PowerShell 逻辑磁盘回退，避免“磁盘实例”为空。
