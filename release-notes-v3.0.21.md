# 观澜 v3.0.21 测试版

修复 Windows 无人值守安装中“配置静默失败”的问题：安装程序报告成功，但机器实际上没有完成配置。

### 修复

1. **`config set` 拒绝全局参数**
   - Windows 安装器以 `guanlan-agent config set --config-root <目录> --hub ... --key-file ...`
     完成自动配置，而 `config set` 的参数解析只认识自己的选项，遇到全局的
     `--config-root` 直接以“未知参数”报错退出。
   - 结果：服务已注册并运行、采集器也在运行，但中枢地址与访问密钥没有写入，
     机器一直向默认地址 `http://127.0.0.1:3100` 重试并堆积待上传样本，
     `/VERIFY` 因此超时。Linux 侧的安装脚本没有传 `--config-root`，所以未受影响。
   - 修复：`config set` 接受并忽略由上层解析的全局参数。
   - 新增 Go 回归测试：通过 `dispatchSubcommand` 走一遍安装器使用的完整命令行，
     断言中枢地址、访问密钥、设备名与"持续采集"默认值都被正确写入；
     另加控制 token 在重复创建时不被轮换的测试。

2. **安装器不再“假装成功”**
   - 当调用方显式传入 `/HUB` 时，如果自动配置失败，安装器现在返回退出码 4
     （非静默时同时弹出说明），不再只打印一行 DetailPrint 后继续报成功。

### 验收状态

- **Linux 无 GUI 验收已全部通过**（安装 → 系统级服务自启 → 桩中枢确认上报 →
  重启后恢复上报 → 卸载保留配置，共 9 步全部 success）。
- Windows 无 GUI 验收在 v3.0.20 的失败已被定位并修复；本次 CI 将重新验证，
  失败时仍会输出状态、服务、机器配置目录与后端日志尾部用于自诊断。
- 发布闸门保持有效：v3.0.19 与 v3.0.20 的验收失败时 `publish` job 均被正确跳过，
  未产出 Release。

本版本仅作为 GitHub prerelease 测试版发布，不更新 `latest`，不用于生产部署。
