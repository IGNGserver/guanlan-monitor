# v0.2.189

### 修复与优化
1. **多显卡探测与并发指标融合支持（独显与核显共存）**：
   - 彻底重构 Agent GPU 采集与多源合并算法（`mergeGPUStats`），消除多显卡采集源之间互斥 fallback 跳过的问题，确保核显（iGPU）与独显（dGPU，如 NVIDIA / AMD）物理显卡实例全部被完整保留并注入对应性能指标（LHM 传感器、nvidia-smi 独显高精度监控及 Windows 性能计数器）。
   - 统一 Windows 下物理显卡实例唯一标识符生成规则，消除硬件传感器探针与硬件探测列表之间的 ID 差异，确保新增探测的独显能够正确通过实例白名单过滤。
   - 增强显卡型号模糊匹配与归一化逻辑，兼容各类带品牌前缀、后缀（如 Laptop GPU / Max-Q）的型号别名。
2. **桌面端本地配置与中枢自动即时同步**：
   - 在桌面端本地配置更新（包括启用设备白名单、指标开启及探测选项）且开启中枢同步时，自动即时触发增量配置同步，确保新增探测实例即刻在中枢生效，无需手动重复点击同步。
   - 修复组件编辑页面“恢复初始”与“添加组件”抽屉中未包含新增独显设备组与图表选项的问题。
