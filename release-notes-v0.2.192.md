### 修复与优化

1. 显卡单卡与多显卡温度隔离彻底修复：
   • 修复桌面端显卡子设备卡片与监控小组件在单卡温度缺失时错误回退至全局汇总 `series.gpuTemperatureC` 的问题，彻底消除核心显卡（Intel UHD）因未配备独立温度传感器而错误套用/显示独立显卡（NVIDIA）温度的问题。
   • 在显卡单卡视图与自定义组件中严格执行实例级数据隔离（`targetId` 匹配），针对无独立传感器的显卡正确展示“未检测到独立温度传感器”占位与空状态提示，杜绝多显卡数据串联。
   • 提取并重构桌面端小组件序列提取辅助模块 `widgetLines.ts`，增加单卡多显卡温度与显存隔离的自动化单元测试覆盖。
