# v0.2.210

- 新增统一温度源采集模型，保留 CPU、GPU、硬盘、主板、SuperIO、PECI、ACPI 和派生温度的原始名称、来源、状态与阈值。
- Windows 继续使用特权硬件传感器缓存补齐 CPU/硬件温度；Linux 采集 hwmon、thermal、GPU 和 SMART 温度源，并保留诊断通道。
- 核显温度继续明确标注为 CPU Package 共享温度，不伪造独立核显温度传感器。
- 控制台新增温度源列表、来源/状态标注和单源历史图表；不同后端不会跨来源平均合并。
