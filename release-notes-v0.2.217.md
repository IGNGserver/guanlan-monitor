# v0.2.217 测试版

## 温度源小组件修复

- 修正温度源目标类型与设备组外壳类型不兼容导致的 TypeScript 构建失败。
- 温度源折线图和饼图继续使用具体传感器选择流程；温度源不是设备组，不会被错误地当作设备组类型渲染。

## 验证范围

- `v0.2.216` 因上述类型边界错误未通过 Release workflow，本版本递增 patch 后重新验证。
- 本地仅执行版本一致性和差异静态检查；完整类型检查、测试、构建和打包由 GitHub Actions 执行。
