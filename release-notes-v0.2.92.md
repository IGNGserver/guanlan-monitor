# v0.2.92 测试版

## 虚拟化平台适配层基础

- 新增统一的虚拟化节点、VM、虚拟磁盘、虚拟网卡、存储和能力状态数据模型。
- Agent 支持通过环境变量启用平台适配器，平台凭据不写入仓库。
- 首批实现 Proxmox VE 集群只读采集：节点、VM 清单、电源状态、vCPU、内存、虚拟磁盘、虚拟网卡、存储和累计磁盘/网络流量。
- 虚拟化数据随 Agent payload 进入中枢实时状态和历史明细；未实现或不可用的能力以显式 issue 返回。
- 增加 PVE 配置解析单元测试，并保留 Windows Agent 后端对虚拟化配置字段的兼容性。
