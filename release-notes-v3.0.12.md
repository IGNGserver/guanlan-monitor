# 观澜 v3.0.12 测试版

这是观澜 Web / Electron 共享 UI v3 的登录表单行为断言修复测试版。

- 使用登录表单显式 `access-key` 控件契约验证匿名状态，避免 Playwright 对共享字段说明文本的 label 解析差异造成误报。
- 保留登录标题、字段、登录动作、空态、在线、会话过期与两轮视觉矩阵证据。
- 保留 v3 的全部页面、Context、CSS、健康统计、Widget 编辑、设备顺序草稿、390px 设置导航、全局搜索及数据/权限契约。

本版本仅作为 GitHub prerelease 测试版发布，不更新 `latest`，不用于生产部署。
