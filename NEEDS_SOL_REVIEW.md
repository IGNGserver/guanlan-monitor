# 需要更强模型/产品负责人复核的事项

日期：2026-08-27

以下项目已经确认存在代码或流程证据，但正确处理需要改变核心安全模型、数据归属、兼容承诺、支持范围或外部发布凭据。当前整改不擅自选择方案，以免破坏现有产品行为或造成不可逆的数据/发布影响。

## 核心安全与数据语义

1. **F-02：身份、设备和凭据模型。** 当前 agent、管理员和 viewer 仍共享 `ACCESS_KEY`，且 agent 凭据没有设备绑定。需要确定独立的 agent enrollment/token、管理员/viewer 会话、轮换与撤销、设备级授权、迁移兼容期，以及如何处理已经部署的 agent。F-03 的服务端会话撤销和空闲超时也应与此统一设计。
2. **F-08：虚拟机删除与重建语义。** 物理设备现在保留 closed tombstone，避免旧状态重新注册；虚拟机是否允许同名/同 ID 自动重开、是否清历史、如何处理迁移，必须由产品确定。
3. **F-11：离线 agent 积压归属。** 当前迁移到当前 Hub 的行为可能造成跨 Hub 数据归属变化。需要决定按原 Hub、设备 enrollment、时间窗口或显式迁移策略处理，并定义失败重试与重复数据规则。
4. **F-12：单实例/多实例架构。** 需要明确同一设备是否允许多个 server/Hub 实例并存，以及本地数据库、端口、锁、跨实例会话和升级时的权威来源；不能仅靠清理旧代码推断。

## 发布身份与兼容范围

5. **F-14：正式签名身份。** Android 临时签名回退已移除，但正式 Android keystore 和 Windows Authenticode 证书仍需在受控 GitHub environment 中配置、轮换和审计。需要确认正式发布身份、证书存储、签名验证和丢失/轮换恢复流程。
6. **F-13/F-15：正式晋级和供应链证明。** 当前 stable 已要求非 prerelease Release 并解析不可变 GHCR digest；正式版是否允许从测试版晋级、是否需要 SBOM/构建证明/人工审批、镜像签名和回滚保留策略，需由发布负责人确定。
7. **L-03：旧客户端和旧安装器。** WinUI/GTK 及旧 Inno Setup 路径仍可能被历史用户使用。需要明确支持矩阵、迁移/卸载策略和是否为旧路径补齐与 Electron 相同的 PawnIO 校验；在此之前不删除或强行改写旧发行链。
8. **L-04：iOS。** 代码中仍有未同步版本和不安全网络/认证边界，但仓库支持范围不明。需要决定修复后继续支持、正式下线并清理，还是隔离为实验代码。
9. **L-05：旧 Web 文件。** 需要确认旧入口是否仍被书签、反向代理或部署脚本引用，再决定删除、重定向还是归档。
10. **L-06：旧 Node Agent。** 需要通过部署清单/遥测确认是否仍有真实安装用户，再决定迁移、保留兼容层或删除孤立类型与代码。
## 数据保留与更新状态

11. **L-01：fanNote。** UI 已不再使用但数据链路仍在。删除字段可能影响旧历史、导出或第三方读取；需要先确定迁移、导出和保留期限。
12. **L-02：Hub 自更新。** 需要定义下载失败、校验失败、重启失败、回滚和恢复状态，或明确删除该功能及其状态字段；不能只把入口隐藏而留下不可达状态机。
