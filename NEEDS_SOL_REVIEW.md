# 需要产品/发布负责人处置的事项

日期：2026-08-28

以下项目有代码或流程证据，但需要核心产品、数据归属、兼容范围或正式发布身份决策。

## 已明确接受的风险

1. **F-02：身份、设备和凭据模型。** 产品负责人已明确决定本轮不修复 device key 问题；agent、管理员和 viewer 继续共享 `ACCESS_KEY`，agent 凭据仍不绑定设备。这是明确接受的高风险，不得在验收中表述为已修复。

## 核心数据语义与架构

2. **F-11：离线 agent 积压归属。** 当前迁移到当前 Hub 的行为可能造成跨 Hub 数据归属变化。需要决定按原 Hub、设备 enrollment、时间窗口或显式迁移策略处理，并定义失败重试与重复数据规则。
3. **F-12：单实例/多实例架构。** 需要明确同一设备是否允许多个 server/Hub 实例并存，以及本地数据库、端口、锁、跨实例会话和升级时的权威来源。

## 发布身份与兼容范围

4. **F-14：正式签名身份。** 测试版 Android 已改为受控 `test-release` environment 的固定签名身份，临时签名回退已移除；正式 Android keystore 和 Windows Authenticode 证书仍需在独立的正式 environment 中配置、轮换和审计。
5. **F-13/F-15：正式晋级和供应链证明。** 当前 stable 已要求非 prerelease Release 并解析不可变 GHCR digest；正式版是否允许从测试版晋级、是否需要 SBOM/构建证明/人工审批、镜像签名和回滚保留策略，需由发布负责人确定。
6. **L-03：旧客户端和旧安装器。** WinUI/GTK 及旧 Inno Setup 路径仍可能被历史用户使用。需要明确支持矩阵、迁移/卸载策略和是否为旧路径补齐与 Electron 相同的 PawnIO 校验。
7. **L-04：iOS。** 代码中仍有未同步版本和不安全网络/认证边界，但仓库支持范围不明；需决定继续支持、正式下线清理或隔离为实验代码。
8. **L-05：旧 Web 文件。** 需要确认旧入口是否仍被书签、反向代理或部署脚本引用，再决定删除、重定向或归档。
9. **L-06：旧 Node Agent。** Hub 已恢复对缺失 `system` 计数器的旧 payload 兼容，但长期支持/退役范围仍需部署清单决定。

## 数据保留与更新状态

10. **L-01：fanNote。** UI 已不再使用但数据链路仍在。删除字段可能影响旧历史、导出或第三方读取；需要先确定迁移、导出和保留期限。
11. **L-02：Hub 自更新。** 需要定义下载失败、校验失败、重启失败、回滚和恢复状态，或明确删除该功能及其状态字段。
