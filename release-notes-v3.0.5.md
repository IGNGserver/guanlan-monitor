# 观澜 v3.0.5 测试版

这是观澜 Web / Electron 共享 UI v3 的视觉矩阵验收修复测试版。

- 保持两轮、五个宽度、四个页面路由的 40 格视觉矩阵和行为断言不变。
- 将矩阵单元改为固定 viewport 截图；页面级全页证据仍由前置总览、设备、设置、详情和状态截图覆盖，避免长遥测页面在每个矩阵格重复整页栅格化而造成 CI 长时间停滞。
- 保留 v3 共享 UI 的全部页面、Context、CSS、健康统计、Widget 编辑、设备顺序草稿、390px 设置导航、全局搜索及数据/权限契约。

本版本仅作为 GitHub prerelease 测试版发布，不更新 `latest`，不用于生产部署。
