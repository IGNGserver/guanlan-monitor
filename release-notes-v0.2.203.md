# v0.2.203

- CLI 配置文件与 TUI 配置能力对齐桌面端 Agent 设置，支持指标、探针、实例和实例指标覆盖。
- 新增 `dsc config validate/import/export/push`，导出配置自动脱敏，导入脱敏配置时保留当前访问密钥。
- 修正云同步开关、显式空指标语义和诊断命令退出码；连接检查可区分设备未登记与认证失败。
- 本地 backend 配置写入改为原子替换并限制文件权限；本地控制 token 改用受限 token 文件传递。
