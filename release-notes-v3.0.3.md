# 观澜 v3.0.3 测试版

这是观澜 Web / Electron 共享 UI v3 的修复测试版。

- 修复共享页面/Context/CSS 拆分后的类型导出与组件引用遗漏。
- 修复 Electron 视觉夹具的 `UpdateInfo` 可选字段契约，使桌面 TypeScript 与双平台 bundle 门禁恢复通过。
- 保留宽屏 Web 空白、全局健康统计、显式 Widget 编辑、设备顺序草稿、390px 设置导航、全局搜索、行为测试和两轮视觉矩阵。

本版本仅作为 GitHub prerelease 测试版发布，不更新 `latest`，不用于生产部署。
