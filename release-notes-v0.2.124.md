# v0.2.124 测试版

## 桌面端硬件实例遥测与交互修复

- “已发现硬件”现在支持逐个实例切换是否上报，并同步到本地 Agent、采集器与中枢配置。
- 设备详情按 CPU、磁盘、网卡、GPU 和风扇实例分别展示时间序列，修复只显示设备汇总的问题。
- 图表默认时间范围改为 5 分钟，补充时间横坐标；鼠标悬停时显示实线选中点、精确时间与对应数据。
- 时间范围改为独立设计的按钮组，并补充 Windows 新配置的默认硬件探针启用策略。
