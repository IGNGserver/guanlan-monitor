# Device State Console v0.2.231（测试版）

## Android 端同步、图表与设备列表改进

- Android 中枢地址允许 HTTP 连接公网地址；仍限制为 HTTP/HTTPS，拒绝嵌入式用户名和密码。
- 设备排序和删除改为显式“编辑设备列表”模式，降低首页误触风险。
- 移除 Android 客户端的 Socket.IO 实时通道与重连状态，统一使用前台 HTTP 周期同步和离线缓存。
- 图表按服务端返回的实际时间范围定位横轴，并在采样存在明显空缺时断开折线；1 分钟和 5 分钟实时数据按时间边界裁剪。
- Android 增加独立温度类别，聚合温度源、CPU、显卡和硬盘温度历史数据。

本版本为测试版，不作为正式稳定安装源。
