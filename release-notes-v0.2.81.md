# v0.2.81 测试版

## 观澜图表与连接设置

- 总览、设备详情和资源指标卡统一使用安卓端观澜图表：面积填充、四条网格线、选中点标记、时间读数和拖拽选点保持一致。
- 中枢地址与访问密钥合并为一次“保存连接”操作；主进程先校验地址，再用统一凭据完成 Hub 认证并同步 Agent 上报凭据，修复 `server url is missing`。
- 移除本机 Agent 页面中的重复通信密钥输入，避免与中枢访问密钥形成两套配置。
- 新增 Agent 上报数据配置：可选择 CPU、GPU、内存、磁盘、网络、风扇指标，以及每类硬件的探针来源和启用状态。
- 补齐检测到的硬件实例展示，并让预览适配器支持保存采集配置。

## 验证说明

这是测试版，不代表稳定发布。构建、打包和 Windows 安装验收由 GitHub Actions 完成。
