# v0.2.213 测试版

## Agent 采集配置

- 修复本机 Agent 指标复选框只保存在页面草稿、离开设置页后恢复旧状态的问题。
- 指标勾选现在会立即写入本机配置，温度传感器选项重新进入页面后仍保持选中状态。
- 连续快速切换指标时按操作顺序串行保存，避免异步更新互相覆盖。
- 将“保存采集配置”明确为保存探针来源和实例覆盖配置。

## 验证范围

- 本地仅执行版本一致性和静态检查。
- Go、桌面端、Web 端构建和打包由 GitHub Actions 执行。
- 发布后使用 workstation 安装包验证版本和安装结果。
