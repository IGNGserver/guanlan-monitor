# Device State Console v0.2.228（测试版）

## Android 遥测明细对齐

- Android 设备详情增加温度源面板，按传感器名称、角色、采集后端、状态和阈值展示，并支持查看单个温度源历史曲线。
- 温度源默认隐藏阈值和无效诊断通道，可手动打开诊断显示，避免把不可用读数混入正常温度信息。
- 内存明细补充提交上限及其历史曲线，和桌面端的提交内存语义保持一致。
- CPU 明细补充 L3 缓存；GPU 明细补充集成/独立类型、显存类型和温度源说明。

本版本为测试版，不作为正式稳定安装源。
