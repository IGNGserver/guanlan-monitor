# 观澜设备状态控制台 UX 审计报告

**审计日期**：2026-09-26  
**审计目标**：降低上手成本、理解成本和学习成本  
**审计范围**：首次使用、信息架构、导航、功能可发现性、操作路径、交互逻辑、反馈、错误提示、术语命名、默认值、设置复杂度、高频任务效率

---

## 执行摘要

观澜控制台是一个功能完整的多设备硬件状态监控系统，技术实现扎实，但在用户体验层面存在多处不必要的认知负担和操作摩擦。核心问题集中在：**首次使用缺乏引导**、**术语命名不一致**、**设置系统过于复杂**、**部分页面价值不明确**。

本报告按优先级从高到低列出 28 个具体问题，每个问题都附带可操作的改进建议。优先解决的问题标记为 P0（阻塞新用户）、P1（显著影响效率）、P2（优化体验）。

---

## P0 级问题：阻塞新用户

### 1. 登录页缺乏"如何获取访问密钥"的引导

**位置**：`apps/web/src/components/login-form.tsx:54-68`

**问题**：
- 登录页要求输入"访问密钥"，但没有告诉用户密钥在哪里获取
- 首次使用者会完全卡在这一步，不知道该怎么办
- supportingText 只说"密钥只用于当前浏览器会话认证"，没有解决"密钥从哪来"的问题

**影响**：新用户可能直接放弃使用

**改进建议**：
```tsx
// 在登录表单下方添加帮助信息
<div className={styles.loginHelp}>
  <p><strong>首次使用？</strong></p>
  <p>访问密钥由中枢管理员提供，通常在中枢部署时生成。</p>
  <p>如果你是管理员，请查看 <a href="https://..." target="_blank">部署文档</a> 了解如何获取或重置密钥。</p>
</div>
```

---

### 2. "访问密钥"概念不直观，缺乏上下文解释

**位置**：`apps/web/src/components/login-form.tsx:57-69`

**问题**：
- "访问密钥"是一个抽象概念，用户可能不理解它和普通密码的区别
- 没有解释为什么用"密钥"而不是"密码"
- placeholder 是"输入访问密钥"，但没有格式提示（如长度、字符类型）

**改进建议**：
- 将"访问密钥"改为更直观的"访问密码"或保持"密钥"但添加图标提示
- 在输入框下方添加格式说明，如"密钥通常为 32 位字母数字组合"
- 考虑添加"显示密钥"切换按钮（当前是纯 password 类型）

---

### 3. 首次登录后没有引导流程

**位置**：全局

**问题**：
- 登录成功后直接跳转到总览页，没有任何引导
- 如果中枢还没有设备接入，用户看到的是"等待设备接入"的空状态
- 用户不知道下一步该做什么：是等待 Agent 上报？还是配置什么？

**改进建议**：
- 首次登录时显示简短的引导弹窗（可跳过、不再显示）：
  - "欢迎使用观澜"
  - "当前还没有设备接入。请确保目标设备已安装并启动 Agent，设备会自动出现在这里。"
  - 提供"查看部署文档"和"我知道了"两个按钮

---

## P1 级问题：显著影响效率

### 4. 术语不一致："实例" vs "设备"

**位置**：多处
- `packages/console-ui/src/workspace/pages/DevicesPage.tsx:89` - 页面标题"全部设备"
- `packages/console-ui/src/workspace/pages/shared.tsx:266` - 表格列名"设备实例"
- `packages/console-ui/src/workspace/pages/shared.tsx:358` - "当前实例"
- `packages/console-ui/src/workspace/pages/DeviceDetailsPage.tsx:308` - "设备"

**问题**：
- 同一个概念在不同地方叫法不同
- 用户会困惑："实例"和"设备"是同一个东西吗？
- 增加认知负担，需要用户自己建立映射

**改进建议**：
- 统一使用"设备"作为主要术语
- 如果需要区分物理设备和逻辑实例，使用"物理设备"和"监控实例"
- 全局搜索替换，确保一致性

---

### 5. 术语不直观："最近心跳"

**位置**：
- `packages/console-ui/src/workspace/pages/shared.tsx:270` - 表格列名"最近心跳"
- `packages/console-ui/src/workspace/pages/DeviceDetailsPage.tsx:322` - "最近心跳"

**问题**：
- "心跳"是技术术语，普通用户可能不理解
- 实际含义是"最后一次通信时间"或"最后在线时间"

**改进建议**：
- 改为"最后在线"或"最后响应"
- 如果需要保留技术感，可以在 tooltip 中说明"心跳：设备定期向中枢报告状态的机制"

---

### 6. "单一资源观察"命名晦涩

**位置**：`packages/console-ui/src/workspace/pages/OverviewPage.tsx:113-117`

**问题**：
- "单一资源观察"这个名字非常晦涩
- 用户需要思考才能理解这是"CPU/内存/磁盘/网络的趋势图"
- 增加认知负担

**改进建议**：
- 改为"资源趋势"或"关键指标趋势"
- 或者更直白："CPU/内存/磁盘/网络趋势"

---

### 7. 设置分区命名混乱，Web 端和 Desktop 端不一致

**位置**：`packages/console-ui/src/workspace/shell/PrimaryNavigation.tsx:9-26`

**问题**：
- Web 端默认设置分区是"中枢状态"，Desktop 端是"通用"
- Web 端有"会话安全"，Desktop 端有"连接与上报"
- 同样的功能在不同平台叫不同名字，增加跨平台用户的认知负担

**改进建议**：
- 统一设置分区命名：
  - "通用" → 保留（两端都有）
  - "外观" → 保留（两端都有）
  - "连接" → 统一（Web 端改"中枢状态"为"连接"，Desktop 端改"连接与上报"为"连接"）
  - "会话" → 合并到"连接"或单独保留
  - "Agent" → 保留（仅 Desktop）
  - "数据与更新" → 保留
  - "快捷键" → 保留
  - "关于" → 保留

---

### 8. 设备详情页选项卡过多，缺乏优先级

**位置**：`packages/console-ui/src/workspace/pages/DeviceDetailsPage.tsx:336-343`

**问题**：
- 选项卡数量由 `DEVICE_DASHBOARD.tabs` 决定，可能有很多个
- 所有选项卡平级显示，没有突出最常用的
- 用户需要逐个点击才能找到想看的内容

**改进建议**：
- 将最常用的选项卡（如"概览"、"CPU"、"内存"）放在前面
- 考虑将次要选项卡（如"风扇"、"温度源"）折叠到"更多"菜单中
- 或者提供"快速概览"选项卡，聚合所有关键指标

---

### 9. Agent 设置页面信息密度过高

**位置**：`packages/console-ui/src/workspace/pages/SettingsPage.tsx:275-449`

**问题**：
- Agent 设置页面有 6 个 Surface 区块，信息密度极高
- 包含：服务状态、身份与节奏、采集策略、上报数据、检测结果、温度探测
- 很多设置项需要技术背景（如"采样间隔"、"探针来源"）
- 新用户容易被吓到

**改进建议**：
- 分为"基础设置"和"高级设置"两个层级
- 基础设置只显示：服务开关、设备显示名、上传开关
- 高级设置折叠显示：采样间隔、探针选择、指标覆盖等
- 添加"推荐设置"按钮，一键恢复默认值

---

### 10. "中枢状态"页面价值不明确

**位置**：`packages/console-ui/src/workspace/pages/HubPage.tsx`

**问题**：
- 这个页面内容很薄弱，主要只是显示连接状态和引导去设备目录
- 与总览页功能有重叠
- 用户会困惑：这个页面和设备列表页有什么区别？

**改进建议**：
- 方案 A：合并到总览页，在总览页顶部显示中枢状态卡片
- 方案 B：保留但增加价值，如显示中枢版本、运行时间、历史连接记录等
- 方案 C：改为"连接管理"，专注于连接配置和诊断

---

## P2 级问题：优化体验

### 11. 命令面板搜索结果没有分组

**位置**：`packages/console-ui/src/workspace/shell/CommandPalette.tsx:66-76`

**问题**：
- 命令列表将设备、页面、设置混在一起显示
- 搜索"设置"会同时出现"打开设置"和所有设置分类
- 结果多了以后难以快速定位

**改进建议**：
- 将结果分组显示：
  - "页面"：总览、设备、中枢状态
  - "设备"：匹配的设备列表
  - "设置"：匹配的设置分类
  - "操作"：刷新、同步等
- 每组有独立的标题和分隔线

---

### 12. 设备表格"管理顺序"入口不够显眼

**位置**：`packages/console-ui/src/workspace/pages/DevicesPage.tsx:103`

**问题**：
- "管理顺序"按钮使用 `variant="quiet"`，视觉权重低
- 用户可能不知道可以自定义设备顺序
- 进入管理模式的反馈不明显

**改进建议**：
- 将"管理顺序"按钮改为 `variant="outlined"` 或添加图标
- 在设备表格上方添加提示："拖拽设备行可调整顺序"（如果支持拖拽）
- 或者在空状态时提示："当前没有自定义顺序，点击'管理顺序'开始"

---

### 13. 删除操作隐藏在 OverflowMenu 中

**位置**：`packages/console-ui/src/workspace/pages/shared.tsx:320`

**问题**：
- 删除操作在三点菜单中，需要两次点击才能触发
- 对于高频操作（如批量管理）效率低
- 新用户可能不知道 OverflowMenu 的存在

**改进建议**：
- 在"管理顺序"模式下，直接在每行显示"上移"、"下移"、"删除"按钮
- 或者添加批量操作模式：勾选多个设备后统一操作
- 保留 OverflowMenu 作为非管理模式的快捷操作入口

---

### 14. 总览页标题动态变化但缺乏视觉区分

**位置**：`packages/console-ui/src/workspace/pages/OverviewPage.tsx:56-64`

**问题**：
- 标题会根据状态变化："中枢连接异常"/"系统状态正常"/"等待设备接入"
- 但标题样式没有变化，用户可能不注意
- 异常状态时应该更醒目

**改进建议**：
- 异常状态时标题使用警告色（如橙色或红色）
- 或者在标题旁添加状态图标（正常：✓，异常：⚠）
- 考虑在页面顶部添加全宽状态条

---

### 15. 错误消息风格不统一

**位置**：`packages/console-ui/src/workspace/context/WorkspaceTypes.ts:103-131`

**问题**：
- 有些错误消息是指令式："请输入中枢地址"
- 有些是描述式："中枢地址无效"
- 长度差异大，有的很长（如"中枢地址无效：请输入以 http:// 或 https:// 开头的合法完整 URL"）

**改进建议**：
- 统一为"问题 + 建议"格式：
  - "中枢地址为空。请输入以 http:// 或 https:// 开头的完整地址。"
- 控制长度在 50 字以内
- 对于长错误，使用主消息 + 详细说明的两段式

---

### 16. 刷新时缺乏全局加载指示

**位置**：`packages/console-ui/src/workspace/shell/AppTopBar.tsx:19`

**问题**：
- 刷新时只有按钮文字从"刷新"变为"保存中"或显示 spinner
- 没有全局的加载指示
- 用户可能不知道数据正在更新

**改进建议**：
- 在顶部栏添加细进度条（类似 YouTube 的红色进度条）
- 或者在页面顶部添加"正在同步..."的临时提示
- 刷新完成后显示"已更新"的短暂提示

---

### 17. "密度"设置概念不直观

**位置**：`packages/console-ui/src/workspace/pages/SettingsPage.tsx:157`

**问题**：
- "界面密度"是一个设计术语，普通用户可能不理解
- 描述太长："自动会根据触摸输入和窗口尺寸放大操作目标；远控手机时可手动选择触摸"
- 选项"舒适"、"紧凑"、"触摸"的含义不直观

**改进建议**：
- 改名为"界面大小"或"控件大小"
- 选项改为："小"、"中"、"大"、"自动"
- 或者使用图标+文字：🔲 紧凑 / 🟦 舒适 / 🟩 触摸

---

### 18. 设备详情页面包屑过于简单

**位置**：`packages/console-ui/src/workspace/pages/DeviceDetailsPage.tsx:300-304`

**问题**：
- 面包屑只有"设备 / 主机名"
- 没有体现设备的状态（在线/离线）
- 没有快速返回总览的入口

**改进建议**：
- 改为"总览 / 设备 / 主机名"
- 在主机名旁添加状态标签
- 或者使用下拉面包屑，可以点击"设备"看到设备列表

---

### 19. 设备状态信息分散在多处

**位置**：`packages/console-ui/src/workspace/pages/DeviceDetailsPage.tsx:312-332`

**问题**：
- 设备状态信息分散在：statusline、facts、banner
- 用户需要阅读多个区域才能了解完整状态
- 信息有重复（如状态在多处显示）

**改进建议**：
- 合并为一个"设备状态卡片"
- 卡片包含：状态标签、Agent 版本、最后在线、数据更新时间
- 异常状态时卡片背景色变化

---

### 20. "待处理事项"概念模糊

**位置**：`packages/console-ui/src/workspace/pages/OverviewPage.tsx:37-38`

**问题**：
- "待处理事项"实际是本机采集问题（`snapshot.localBackend?.lastIssueCount`）
- 名字太模糊，用户不知道具体指什么
- 点击后没有跳转到问题详情

**改进建议**：
- 改名为"本机采集问题"或"Agent 异常"
- 点击后跳转到 Agent 设置页面或显示问题详情弹窗
- 如果没有问题，显示"✓ 无异常"而不是"0"

---

### 21. 总览页和设备列表页功能重叠

**位置**：
- `packages/console-ui/src/workspace/pages/OverviewPage.tsx:105-110`
- `packages/console-ui/src/workspace/pages/DevicesPage.tsx`

**问题**：
- 总览页有"异常与最近设备"列表
- 设备列表页有完整的设备表格
- 两个页面都显示设备，但视角不同，增加认知负担

**改进建议**：
- 总览页专注于全局健康状态和趋势图
- 设备列表只出现在设备列表页
- 或者总览页只显示"重点设备"（离线、异常），不显示全部设备

---

### 22. 设置页面描述过长

**位置**：`packages/console-ui/src/workspace/pages/SettingsPage.tsx:39-47`

**问题**：
- 设置分类的描述有些很长，如"浏览器端的刷新、实例筛选和中枢状态"
- 描述应该是简短的补充说明，不是详细说明

**改进建议**：
- 控制描述在 20 字以内
- 例如："浏览器端的刷新、实例筛选和中枢状态" → "浏览器端同步与显示偏好"

---

### 23. "中枢顺序"列名不直观

**位置**：`packages/console-ui/src/workspace/pages/DeviceDetailsPage.tsx:323`

**问题**：
- "中枢顺序"是一个内部概念
- 用户不理解这是"设备在列表中的显示顺序"

**改进建议**：
- 改为"显示顺序"或"排序位置"
- 或者直接不显示这个数字，只在拖拽排序时临时显示

---

### 24. 流量日历控件不够直观

**位置**：`packages/console-ui/src/workspace/pages/shared.tsx:517-546`

**问题**：
- "上一周期"、"下一周期"按钮文字较长
- 没有明确说明当前查看的是什么时间范围
- 日/周/月切换不够醒目

**改进建议**：
- 使用图标按钮：◀ 2026年9月 ▶
- 当前时间范围用大字体显示
- 日/周/月使用 SegmentedControl 并添加图标

---

### 25. 设备表格列含义需要解释

**位置**：`packages/console-ui/src/workspace/pages/shared.tsx:264-272`

**问题**：
- 表格列名如"CPU"、"内存"、"磁盘"没有单位或格式说明
- 用户需要看数据才能理解显示的是什么（如使用率还是频率）
- "最近心跳"列显示的是时间，但列名没有体现

**改进建议**：
- 列名添加单位或说明：
  - "CPU" → "CPU 使用率"
  - "内存" → "内存使用"
  - "磁盘" → "磁盘使用"
  - "最近心跳" → "最后在线"
- 或者在列头添加 tooltip 说明

---

### 26. 快捷键设置页面只是只读列表

**位置**：`packages/console-ui/src/workspace/pages/SettingsPage.tsx:459-462`

**问题**：
- 快捷键设置只是显示当前快捷键，不能修改
- 页面标题是"快捷键"，但实际是"快捷键列表"
- 用户可能期望可以自定义

**改进建议**：
- 改名为"快捷键参考"或"键盘快捷键"
- 或者支持自定义快捷键（长期目标）
- 添加"复制全部"按钮，方便用户导出

---

### 27. "关于观澜"页面信息不够完整

**位置**：`packages/console-ui/src/workspace/pages/SettingsPage.tsx:464-467`

**问题**：
- 只显示版本和发布通道
- 没有显示构建时间、提交哈希等调试信息
- 对于问题反馈，这些信息很有用

**改进建议**：
- 添加"复制版本信息"按钮
- 显示：版本号、构建时间、提交哈希、运行环境
- 添加"检查更新"按钮（当前只在有更新时显示）

---

### 28. 命令面板 placeholder 过长

**位置**：`packages/console-ui/src/workspace/shell/CommandPalette.tsx:119`

**问题**：
- placeholder 是"设备名、ID、页面、设置或操作"
- 太长，用户一眼看不完
- 应该更简洁

**改进建议**：
- 改为"搜索设备、页面或操作"
- 或者"输入设备名、命令或设置"

---

## 高频任务效率分析

### 任务 1：查看某台设备的详细状态

**当前路径**：总览 → 设备列表 → 点击设备 → 设备详情（3 步）

**优化建议**：
- 在总览页的"重点设备"列表直接点击可跳转到详情
- 或者在命令面板搜索设备名直接跳转
- 当前已经支持，但入口不够明显

### 任务 2：刷新数据

**当前路径**：点击刷新按钮 或 F5（1 步）

**评价**：路径已经很优化

### 任务 3：修改 Agent 设置

**当前路径**：设置 → Agent → 找到具体设置项 → 修改（3+ 步）

**优化建议**：
- 常用设置（如开关 Agent、修改设备名）应该在顶部
- 或者在设备详情页直接提供"编辑此设备 Agent 设置"的快捷入口

### 任务 4：查看设备趋势

**当前路径**：设备详情 → 选择选项卡 → 选择时间范围（3 步）

**优化建议**：
- 在设备列表页的表格行直接显示迷你趋势图
- 或者在总览页提供"快速趋势"入口

---

## 信息架构优化建议

### 当前信息架构

```
总览
├── 全局健康状态
├── 重点设备列表
└── 单一资源观察图表

设备
├── 设备表格（搜索、筛选、排序）
└── 设备详情
    ├── 选项卡（概览、CPU、内存、磁盘、网络、GPU、风扇、温度）
    └── 时间范围控制

中枢状态
├── 连接状态
└── 引导去设备目录

设置
├── 通用 / 中枢状态（平台相关）
├── 外观
├── 连接 / 会话安全
├── Agent（仅 Desktop）
├── 数据与更新
├── 快捷键
└── 关于
```

### 建议的信息架构

```
总览
├── 中枢状态卡片（合并"中枢状态"页的核心信息）
├── 全局健康统计
├── 资源趋势图（改"单一资源观察"）
└── 异常设备提醒（不显示全部设备）

设备
├── 设备表格（搜索、筛选、排序、管理）
└── 设备详情
    ├── 快速概览（新增：聚合关键指标）
    ├── 选项卡（按使用频率排序）
    └── 时间范围控制

设置
├── 通用（两端统一）
├── 外观（两端统一）
├── 连接（统一命名）
├── Agent（仅 Desktop，分基础/高级）
├── 数据与更新
├── 快捷键参考（改名）
└── 关于（增加版本详情）
```

---

## 术语统一表

| 当前用法 | 建议统一 | 说明 |
|---------|---------|------|
| 实例 / 设备 | 设备 | 统一使用"设备"，避免混用 |
| 最近心跳 | 最后在线 | 更直观 |
| 单一资源观察 | 资源趋势 | 更简洁 |
| 中枢顺序 | 显示顺序 | 更直观 |
| 待处理事项 | 本机采集问题 | 更明确 |
| 密度 | 控件大小 | 更直观 |
| 快照 / 状态 | 状态 | 统一使用"状态" |
| 同步 / 刷新 | 刷新 | 统一使用"刷新" |

---

## 优先级实施路线图

原计划分三阶段推进；实际在同一天一次性落地（见文末「实施结果」），此处保留优先级判断作为依据。

### 第一阶段（阻塞新用户）
1. 登录页添加「如何获取密钥」帮助信息 ✅
2. 首次登录后添加简短引导 ✅
3. 统一术语：「实例」→「设备」，「心跳」→「最后在线」 ✅

### 第二阶段（显著影响效率）
4. 统一设置分区命名 ✅
5. 简化 Agent 设置页面（分基础/高级）✅
6. 重命名「单一资源观察」为「资源趋势」✅
7. 评估「中枢状态」页面的去留 ✅（结论：并入总览 + 设置·连接，页面删除）

### 第三阶段（体验优化）
8. 命令面板结果分组 ✅
9. 优化设备表格操作入口 ✅
10. 统一错误消息风格 ✅
11. 添加全局加载指示 ✅


---

## 结论

观澜控制台的核心功能完整，技术实现扎实，但在用户体验层面存在多处可以优化的地方。主要问题集中在：

1. **首次使用缺乏引导**：新用户不知道如何获取密钥、登录后该做什么
2. **术语不一致**：同一概念在不同地方叫法不同，增加认知负担
3. **设置系统复杂**：Agent 设置页面信息密度过高，缺乏分层
4. **部分页面价值不明确**："中枢状态"页面功能单薄

通过本报告的改进建议，可以显著降低上手成本、理解成本和学习成本，让新用户更容易理解，老用户操作更高效。

---

**报告完成**：2026-09-26  
**审计方法**：代码审查 + 用户流程分析 + 启发式评估  
**审计人**：UX Audit Agent

---

## 附：实施结果（2026-09-26 同日落地）

28 项中 25 项已按建议实施，2 项因审计前提有误而改为更小的修正，1 项经判断后未实施。以下逐条记录，避免把「已改动」当成「已解决」。

### 已实施

| 问题 | 落地方式 |
|---|---|
| 1/2 登录页密钥来源 | `login-form.tsx` 新增「还没有访问密钥？」说明块：明确密钥由中枢管理员设置、不是设备登录密码、可在 `ACCESS_KEY` 查看，并给出部署文档入口；加「显示密钥」切换 |
| 3 首次引导 | 新增 `shell/OnboardingGuide.tsx`：桌面 3 步（连接中枢→启动 Agent→等待上报）、浏览器 3 步（输入密钥→等待上报→打开目录），可关闭并写入 `dsc-onboarding-dismissed`，仅在设备数为 0 时出现 |
| 4 实例/设备 | 表头「设备实例」→「设备」，总览摘要「当前实例」→「设备总数」；「实例」只保留在真正指代机内硬件的位置（风扇实例、全部实例、逐实例上报） |
| 5 心跳 | 表头「最近心跳」→「最后在线」，行内状态「当前响应/心跳已过期」→「刚刚上报/已停止上报」 |
| 6 单一资源观察 | 改为「资源趋势」，副标题改为「每台设备一条数据线」 |
| 7 设置分区 | 两端统一为 通用/外观/连接/数据与更新/快捷键参考/关于观澜（桌面多一项 本机 Agent）；`#settings/workspace`→通用、`#settings/session`→连接 作为旧地址别名保留 |
| 9 Agent 设置分层 | 拆为「服务状态 / 这台设备 / 采集策略」常显 + `<details>` 折叠的「高级设置」（采样间隔、逐项指标、探针来源、硬件实例、温度源）；折叠区内所有开关仍是改即存，不会因隐藏而丢改动 |
| 10 中枢状态页 | 删除 `HubPage.tsx` 与 `hub` 路由种类，`#hub/*` 降级为总览别名；连接事实（状态/地址/最近同步/设备范围）改为总览页 `HubStatusCard`，诊断与会话归入 设置·连接 |
| 11 命令面板分组 | 结果按 页面/操作/设置/设备 分组并显示组名，排序即分组顺序 |
| 12/13 目录操作 | 「管理顺序」改为带图标的高对比按钮；管理模式下每行直接暴露 上移/下移/删除 图标按钮（删除不再藏在三点菜单后），并加草稿说明条 |
| 14 标题状态区分 | `PageIntro` 新增 `tone`，异常时标题转为警示色并带图标 |
| 15 错误文案 | `formatWorkspaceError` 全量重写为「事实。下一步。」两段式并统一长度；补 `desktop_bridge_not_ready` 映射 |
| 16 全局反馈 | 顶栏下方新增刷新进度条；新增 `CopyButton` 带「已复制/复制失败」状态（复制失败不再与成功同貌） |
| 17 控件大小 | 「界面密度」→「控件大小」，选项按尺寸重排为 自动/紧凑/标准/触控，说明改为一句人话 |
| 18/19 设备详情 | 面包屑补「总览」层级；状态行与事实条合并为单张事实卡（7 项：设备状态/数据链路/最后在线/数据时间/Agent 版本/发布通道/列表位置），不适用指标改为独立说明条 |
| 20 需要关注 | 摘要第 3 格改名「需要关注」，并把构成写明「N 台未响应 · M 条本机采集问题」；连接异常时显示「无法判断」而非 0 |
| 21 总览/目录重叠 | 总览只列未响应设备，全在线时明确写「N 台设备全部在线，无需处理」并指向设备页 |
| 22 设置描述 | 各分区描述压到 12 字左右 |
| 23 中枢顺序 | 改名「列表位置」 |
| 25 列名语义 | CPU/内存/磁盘 列改为「CPU 使用率 / 内存使用 / 磁盘使用」 |
| 26 快捷键 | 改名「快捷键参考」并加「复制全部快捷键」 |
| 27 关于 | 加「重新检查更新」和「复制版本信息」 |
| 28 placeholder | 缩为「搜索设备、页面或操作」（触发按钮的可访问名保持原样，不动既有契约） |

### 审计前提有误，改为更小修正（2 项）

**问题 8（选项卡过多）**：核实 `DEVICE_DASHBOARD.tabs` 实际只有 4 项（概览/处理器与内存/存储与网络/显卡与散热），顺序也已是高频优先。这是我在上一版审计中的误判——报告里写的是「取决于 tabs 可能很多」。未做任何折叠，改为在报告与代码注释里明确它就是 4 项固定布局。

**问题 24（流量日历说不清当前范围）**：卡片副标题已经在显示 `data.title · 起 至 止`，信息并不缺。实际缺陷只是「上一周期/下一周期」两个文字按钮过宽。改为图标按钮并保留 `aria-label`。

### 判断后未实施（1 项）

**问题 13 的「批量勾选删除」分支**：审计报告提过两种方案。批操作会引入选择态、全选栏、误删面，与本任务「降低理解成本」的目标相反，故只做了「管理模式直接暴露行操作」这一半。

### 顺带修掉的真实缺陷

1. **表格列渲染的隐性错位**：`CarbonDeviceTable` 原先用 `cell.info.header` 去匹配 `"status"`、`"device"` 这类 **key**，而该字段实际是显示文案。改成表头与单元格共用同一份列定义（`DirectoryColumn[]`），从结构上消除再次错位的可能。
2. **`<Button aria-label>` 被静默丢弃**：TypeScript 允许任何带连字符的 JSX 属性通过，所以 `deviceCharts.tsx` 的「复制设备 ID」图标按钮此前**没有可访问名**。现在 `Button` 显式接收并转发 `aria-label`。
3. **390px 工具栏断言失败（main 上连续 5 次 CI 红的成因之一）**：`≤672px` 时 sort 与 actions 已在同一网格行，但 `align-items: center` 让两者顶边相差数像素。改为 `align-items: start`，并把修复写在属主文件 `workspace-carbon.scss`——我先误加在 `workspace.pages.css` 的覆盖层已撤销，避免重现「同一个类名多层互相打架」。

### 契约同步与验证

- 同步了 `scripts/visual-regression.cjs`、`scripts/electron-visual-regression.cjs`、`routes.test.ts`、`selectors.test.ts` 里被改名钉住的文案，并**新增**断言：导航不得残留「中枢状态」、总览必须有中枢卡、「需要关注」必须写明构成、两端设置分区词表一致、首次引导的出现与关闭持久化、管理模式首行不可再上移。
- 本机已验证（不产生交付物）：console-ui 与 web 登录页 TypeScript 类型检查通过；26 项工作区契约测试全绿；`check:desktop-ui-boundaries`、`check:web-ui-boundary`、`check:adapter-contracts`、`verify:version` 全部通过；两个 `.cjs` 脚本语法检查通过；CSS 花括号配平。
- 本机**无法**验证：构建产物与浏览器内实际渲染。按仓库要求走 CI；`pnpm install` 与本地构建在本仓库的 CIFS 挂载上被禁止。

### 遗留项（明确未做，非遗漏）

1. **死 CSS 待一次集中清理**。本次让若干类失去引用，但我没有在多个层里逐个删——`workspace-carbon.scss`、`workspace.responsive.css`、`workspace.shell.css`、`workspace.m3.css` 正被另一会话编辑，跨层半删会重新制造「同一个类名被多层定义」的问题。当前实测无 TSX 引用的类：
   - `.workspace-overview-grid`（含 `--single`/`--v3` 变体）：样式散在 5 个文件——`workspace.m3.css`、`workspace.shell.css`、`workspace.dashboard.css`、`workspace-carbon.scss`、`workspace.pages.css`
   - `.workspace-web-settings*`（旧 web 中枢状态块）：`workspace.dashboard.css`
   - `.workspace-device-statusline`（详情页状态行，已并入事实卡）：`workspace.m3.css`、`workspace.dashboard.css`
   - `.workspace-hub-facts`（随中枢页删除）：`workspace-carbon.scss`（我在自己所属的 `workspace.pages.css` 内已删）
   - `.workspace-hub-next-step`、`.workspace-collection-surface`：已无样式定义，仅历史遗留
2. **可自定义快捷键**（问题 26 的长期解）：需要快捷键绑定层与冲突检测，本次只把命名改成事实（「参考」）。
3. **Android 端术语**：`android/app/src/main/java/com/dsc/android/ui/AppRoot.kt` 仍有「删除设备实例？」「中枢连接与上报设置」，属另一会话正在推进的 Android 改动范围，未跨端改词。
4. **版本号与发布归属**：`VERSION` 与 6 个 manifest 的 3.0.108 是另一会话**未提交**的改动，HEAD 仍是 3.0.107。本次提交（`1e7ec0a`）不含版本递增，也没有打 tag、没有测试版 Release——两路会话抢同一个 patch 号会破坏 AGENTS.md 的发布轨迹，需要先协调。

