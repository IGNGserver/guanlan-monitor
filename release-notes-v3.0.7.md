# 观澜 v3.0.7 测试版

这是观澜 Web / Electron 共享 UI v3 的行为测试稳健性修复测试版。

- 将设备删除确认测试限定在删除对话框内，避免与设备顺序草稿的“取消”动作发生 strict-mode 歧义。
- Web 视觉 runner 在断言失败时也会关闭 Playwright 浏览器，确保 CI 明确结束并上传可审计日志。
- 保留直添 Widget 草稿保存/放弃契约、两轮 40 格视觉矩阵，以及 v3 的全部页面、Context、CSS、健康统计、设备顺序草稿、390px 设置导航、全局搜索和数据/权限契约。

本版本仅作为 GitHub prerelease 测试版发布，不更新 `latest`，不用于生产部署。
