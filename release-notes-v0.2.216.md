# v0.2.216 测试版

## 温度源小组件

- 新增温度源折线图和饼图，添加时先选择具体温度传感器，不再直接创建空的总温度组件。
- 温度源目标列表同时使用历史序列和最新采集结果，刚启用温度采集也可以选择已有有效传感器。
- 饼图按当前温度与高温/临界阈值展示余量；没有阈值时使用 100 °C 作为参考刻度。
- 兼容旧版本已经添加的温度源总表，并在缺少最新快照时回退到历史温度序列。

## 验证范围

- 本地仅执行版本一致性、差异和工作区静态检查。
- TypeScript、测试、桌面端和 Web 端构建及打包由 GitHub Actions 执行。
- 发布后使用 workstation 的 Windows GUI setup 验证安装结果。
