# Device State Console v0.2.65（测试版）

本版本是统一 Windows/Linux Electron 桌面客户端的测试版验证发布。

- Windows GUI setup / portable / update
- Linux GUI deb
- Windows/Linux CLI install assets
- Android、iOS GUI 资产
- Electron + React/TypeScript renderer、Agent lifecycle、Hub session、缓存与安全边界

本版本仅用于测试，不覆盖 `latest`，不代表正式稳定发布或生产部署。
