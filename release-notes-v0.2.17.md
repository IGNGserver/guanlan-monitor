# v0.2.17

## 界面彻底重构与排版 Bug 修复
- **解决“本机控制”卡片重叠黑块**：重构 `LocalControlPanel` 布局为 StackPanel 垂直流容器，彻底避免面板在 Grid 中强行塞入 Row=0 发生的粘连混乱。
- **解决 TaskManager 图表全黑与数据显示 `--`**：通畅选定设备到大屏的数据管道，自动解包节点硬件型号名（如 `Intel Core i9-14900HX`），修复 Canvas 动态重绘，在有数据时绘制平滑渐变折线、无数据时展现灰网格。
- **修复集群总览页 (OverviewPage) 排版**：修复卡片内文字上下粘连挤压，标准化 TitleBar 背景与全屏弹域，消除右上角白块遮挡。
