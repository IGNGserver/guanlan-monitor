# 观澜 v3.0.88 测试版

## 虚拟机功能移除文档收尾

- README 不再将虚拟机监控描述为当前产品能力。
- 将旧审计和 UI 设计文档标明为历史记录，避免把移除前的描述误认为当前功能。
- v3.0.87 已移除虚拟机采集、服务端接口与模型，以及 Web、Electron、Android 和 iOS 的相关界面。

本版本仅作为 GitHub prerelease 测试版发布，不更新 `latest`，不用于生产部署。
