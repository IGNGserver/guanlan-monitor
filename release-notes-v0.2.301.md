# v0.2.301 测试版

- 修复测试渠道 Compose 固定启用 Agent HTTPS 预检，恢复可信内网 HTTP Agent 的数据上报。
- 稳定生产渠道仍强制 Agent 使用 HTTPS；Compose 现在按渠道和环境配置传递该开关。
- Android/桌面端通过公网入口读取中枢时，设备状态恢复后不再因无实时数据触发详情 404。
- 本版本是测试版，不代表正式生产发布。
