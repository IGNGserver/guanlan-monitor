# v0.2.183

### 改进与修复
1. **修复“处理器与系统统计”区块高度不足与显示不全**：
   - 修正 `compute-cpu-facts` 小组件的栅格高度跨度为 2 行（`--widget-h: 2`），并优化其内边距与子项流式排布，解决在 7 项系统统计数据换行展示时卡片高度不足、导致底部数据与顶部小标题溢出截断的问题。
2. **优化 Windows GUI 安装器更新后退出流程**：
   - 优化 NSIS 安装器脚本，当检测到更新前软件处于打开状态或托盘驻留状态时，在启动恢复软件后立即自动退出安装器（`Quit`），不再停留于完成页等待用户手动关闭。
