# v0.2.108 测试版

## 虚拟机实例

- 普通设备与虚拟机实例在网页、桌面端、Android、iOS 列表中分栏切换。
- 虚拟机由宿主机上报自动登记，支持多宿主机关联、宿主机名称显示、独立排序和软删除。
- 删除后的虚拟机在列表中隐藏；宿主机再次发现并上报后自动恢复显示并保留原排序。
- 虚拟机复用普通设备的 CPU、内存、硬盘、网络指标接口、历史数据和图表详情。
- 增加 MySQL `virtual_machines` 表及本地 JSON 存储实现。
