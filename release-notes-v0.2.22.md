# v0.2.22

## 数据严谨真实化与多硬件设备选择器
- **绝对杜绝伪造假数据**：全面清理代码中为了展示而充填的任何伪造/硬编码假字符串（如写死的 MHz、线程数、IP 地址、型号等）。凡是后端传输包未采集到的硬件字段，严谨统一下沉显示为 `"--"`。
- **按设备精度精细化选择（如多网卡/多磁盘/多显卡）**：在 TaskManager 主界面顶部新增【子设备选择器 ComboBox】。默认显示“全部设备”综合缩略图，用户可通过下拉框精准选择具体的子设备（如 `[Wi-Fi]`, `[Disk 0 (C:)]`, `[NVIDIA RTX 2050]`），图表与各项指标将 100% 切换为该具体设备的真实监控数据。
