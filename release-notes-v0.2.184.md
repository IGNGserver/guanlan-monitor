# v0.2.184

### 改进与修复
1. **修复“处理器与系统统计”组件在布局引擎中的高度硬编码**：
   - 彻底修复 `WidgetLayout.tsx` 布局引擎与网格归一化算法中对 `compute-cpu-facts` 强制注入 `h: 1` 的逻辑，将其同步修正为标准的 2 行跨度（`h: 2`）。
   - 确保 `DesktopWidget` 渲染的内联 `--widget-h` 变量及网格占位坐标正确计算为 2 行，使 7 项系统事实卡片和卡片标题获得充足垂直空间，彻底消除任何裁剪和溢出。
