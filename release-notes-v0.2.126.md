# v0.2.126

- 修复桌面端自动刷新：定时刷新会重新读取本机 Agent 和中枢数据，不再停留在旧快照。
- 修复“已发现硬件”实例开关：关闭或开启后立即写入本机配置，重新进入页面保持选择。
- 修复待上传样本状态：按实际 JSONL 队列重新核对数量和字节数，并在中枢地址变更后使用当前地址重放队列。
- 增加内存、磁盘已用容量/总容量展示及容量趋势图，补充历史记录的容量回退。
- 隔离 CPU、磁盘、网络、GPU 慢速采集与硬件探测故障，避免单项失败导致其他设备图表归零或检测结果丢失。
- 保留图表时间轴、悬浮时间和值标签、独立设备图表、型号列表及非零谷值统计修复。
