# 观澜 v0.1.100

## Windows

- 按硬盘分别查看已用容量、读写速率和温度历史。
- 按显卡分别查看显存已用容量历史与当前显存总容量。
- 实例级 CPU、硬盘、网卡、显卡和风扇数据与中枢历史记录保持一致。

## Android

- 中枢地址示例统一为 `http://服务器IP:3100` 或 `https://你的域名`。
- 已保存的旧 `:4000`、`:3101` 地址会自动迁移为 `:3100`。

## 安装包

- Windows setup、Windows update ZIP 与已签名 Android APK 均包含在本 Release 中。
