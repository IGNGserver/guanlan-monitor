# v0.2.256

## 安卓端显示虚拟化存储池

- 补齐 Android 对当前存储池和历史容量序列的 API 数据映射。
- 在 Android 硬盘详情的总览页展示 `local-lvm`、`pve-ssd` 等有效存储池的容量、可用空间和使用率。
- 对 Proxmox 未提供的存储池读写速率显示“无法获取数据”，不再将缺失值显示为 `0`。
- 沿用节点范围和有效容量过滤，隐藏其他集群节点或无数据的存储池。

## 校验

- 构建、测试、打包、Release 资产和部署验证由 GitHub Actions 流程执行。
