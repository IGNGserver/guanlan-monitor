# v0.2.302 测试版

- 修复 Linux Agent 将无效 hwmon 温度值（例如 `-125°C`）放进上报 payload，导致整条 telemetry 被中枢拒绝的问题。
- 重放旧待上传队列时自动清洗同类无效温度值，避免历史队列阻塞 Agent 恢复在线。
- 测试渠道继续允许可信内网 HTTP Agent；稳定生产渠道仍强制 HTTPS。
- 本版本是测试版，不代表正式生产发布。
