# Device State Console v0.2.230（测试版）

## CLI 探测请求超时修复

- CLI 对 `/api/probes/detect` 使用 45 秒请求窗口，与桌面端保持一致，避免硬件探测耗时超过 5 秒时被提前取消。
- 普通 CLI backend 请求仍保持 5 秒超时，避免异常 backend 请求长时间阻塞。
- 增加端点超时选择和慢请求取消回归测试。

本版本为测试版，不作为正式稳定安装源。
