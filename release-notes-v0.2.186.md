# v0.2.186

### 改进与修复
1. **修复 5 分钟与 1 小时时间范围视图数据截断不全问题**：
   - 服务端扩容并支持 `5m` 高分辨率实时时序桶（保留 150 个原始采样点），修复原本 `5m` 回退至 12 个点（仅 1 分钟）导致图表左侧 80% 空白的问题。
   - 服务端在 `getSeries` 中将 `1h`（及 `15m`）接入 `device_minute_metrics` 分钟级历史时序查询，并在 MySQL / Local 仓库中解除对 `1h` 与 `15m` 的空数组硬编码拦截，确保 1 小时视图完整返回 60 个分钟聚合点并融合当前在途聚合。
   - 完善 Redis 实时清理逻辑，支持同步清理 `5m` 时序缓存。
