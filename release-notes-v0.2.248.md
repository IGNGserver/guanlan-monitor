# v0.2.248

## 网页端中枢工作台

- 重做浏览器端登录页，统一当前中枢工作台的暗色视觉、会话提示和访问密钥表单。
- 将网页端工作区限制在浏览器视口内，让侧边栏固定在左侧，右侧内容独立滚动。
- 新增适合浏览器中枢的工作台、会话安全、同步状态和实例偏好页面。
- 隐藏网页端不适用的本机 Agent、启动行为、窗口控制和桌面连接配置入口。

## 校验

- Web UI、桌面 UI 边界和适配器契约静态检查通过。
- `packages/console-ui` 与 `apps/web` TypeScript `noEmit` 检查通过。
